import 'package:flutter/material.dart';

class LaunchDetailsScreen extends StatelessWidget {
  const LaunchDetailsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Details Screen'),
    );
  }
}
